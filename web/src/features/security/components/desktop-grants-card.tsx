// Compatibility export for security-scoped consumers. The profile surface is
// the canonical implementation and remains shared by both routes.
export { DesktopGrantsCard } from '@/features/profile/components/desktop-grants-card'
